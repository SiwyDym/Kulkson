<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.team.kulkson" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:screenOrientation="landscape"
            android:name=".activity_opcje"
            android:label="@string/title_activity_activity_opcje" >
        </activity>
        <activity

            android:name=".KScore"></activity>
        <activity android:name=".KOpcje"></activity>
        <activity android:name=".MainMenu"></activity>

        <activity android:name=".KGame">



        </activity>
        <service android:name=".KMusic"></service>

    </application>

</manifest>
